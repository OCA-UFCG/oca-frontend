"use client";

import { MapWrapper } from "@/app/globalStyles";

const MapTemplate = ({ children }: React.PropsWithChildren) => {
  return <MapWrapper>{children}</MapWrapper>;
};

export default MapTemplate;
